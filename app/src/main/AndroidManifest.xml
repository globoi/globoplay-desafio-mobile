<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nroncari.movieplay">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".di.AppApplication"
        android:allowBackup="true"
        android:icon="@drawable/movieplay_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/movieplay_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.Movieplay">

        <activity
            android:name=".presentation.ui.NavigationActivity"
            android:theme="@style/AppTheme.Splash"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".presentation.ui.YoutubePlayerActivity"
            android:configChanges="orientation|screenSize|keyboardHidden|smallestScreenSize|screenLayout"/>
    </application>

</manifest>