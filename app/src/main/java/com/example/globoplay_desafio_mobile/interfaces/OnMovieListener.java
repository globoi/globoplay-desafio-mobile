package com.example.globoplay_desafio_mobile.interfaces;

import com.example.globoplay_desafio_mobile.models.movies.MovieModel;

public interface OnMovieListener {
    void onMovieClick(MovieModel movie); //when the user clicks on the movie
}
